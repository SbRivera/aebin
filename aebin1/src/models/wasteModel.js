export const processWasteData = (rawData) => {
    // Transforma rawData en el formato requerido para los gráficos, etc.
    // Esta función es un placeholder para tu lógica de negocio.
    return rawData;
  };

  